

export interface TopSellingMenuItemsProps {
  itemName: string;
  money: number;
}

export const TopSellingMenuItems = ({
  data,
}: {
  data: TopSellingMenuItemsProps[];
}) => {
  return <div>TopSellingMenuItems</div>;
};
